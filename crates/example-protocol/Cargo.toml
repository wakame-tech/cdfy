[package]
name = "example-protocol"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
fp-bindgen = { git = "https://github.com/fiberplane/fp-bindgen", features = ["generators"] }
once_cell = "1.17.0"